{
  "name": "salesforcestreaming",
  "version": "1.0.0",
  "description": "Server to listen for specific SFDC changes (specifically on the lead object) and using that information to make changes within a Drift instance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsforce": "^1.10.1",
    "node": "^16.9.1",
    "superagent": "^6.1.0"
  }
}
